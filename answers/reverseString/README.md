# 反转字符

#### 说明

Given a string, return a new string with the reversed order of characters

#### 示例

reverse('apple') === 'leppa'

reverse('hello') === 'olleh'

reverse('Greetings!') === '!sgniteerG'
